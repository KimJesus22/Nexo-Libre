/**
 * Hooks personalizados de React.
 *
 * Cada hook se coloca en su propio archivo dentro de este directorio.
 * Los hooks deben usarse exclusivamente en Client Components ('use client').
 *
 * Convención de nombres: useNombreDescriptivo.ts
 *
 * Ejemplo:
 *   import { useMediaQuery } from '@/hooks/useMediaQuery'
 */
