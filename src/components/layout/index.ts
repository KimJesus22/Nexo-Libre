/**
 * Componentes de layout reutilizables.
 *
 * Aquí se colocan componentes estructurales que se comparten
 * entre múltiples páginas: cabecera, pie de página, barra lateral,
 * contenedor principal, etc.
 *
 * Ejemplo:
 *   import { Cabecera } from '@/components/layout/Cabecera'
 */
